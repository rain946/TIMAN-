services:
  db:
    image: mysql:8.0
    container_name: timan_mysql
    restart: unless-stopped

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: timan_db

    ports:
      - "3306:3306"

    volumes:
      - timan_mysql_data:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql

    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-h",
          "localhost",
          "-uroot",
          "-proot",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: ./backend
    container_name: timan_api
    restart: unless-stopped

    ports:
      - "3000:3000"

    environment:
      PORT: 3000
      DB_HOST: db
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: root
      DB_NAME: timan_db
      JWT_SECRET: timan_development_secret

    volumes:
      - ./backend/uploads:/app/uploads

    depends_on:
      db:
        condition: service_healthy

volumes:
  timan_mysql_data:
